# ScamShield AI — Environment variables
# Copy this file to .env.local and fill in values as needed.
# Do NOT commit real keys.

# AI provider key (server-side only, added in a later stage).
OPENAI_API_KEY=
